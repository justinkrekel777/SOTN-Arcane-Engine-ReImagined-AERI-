PATCH NOTES Version 1.1.2 5/13/2026


Bug fixes:

    1. fixed a visual bug introduced in the 1.1 release that allowed critical arts cooldown to run even without a spec weapon equipped
    2. fixed issues with some shield equip IDs not being flagged properly in the shield dash run check
    3. fixed an issue where the katana was classed under the wrong spec weapon moveset
    4. fixed signing error on wingsmash buffer clamp equation that would cause execution to get stuck on when using high buffer values
    5.


Additions/reworks:

    1. added normal back dash chaining when not holding a shield in the left hand (QoL for people that want to use alternate loadouts)
    2. added fast back dash chaining when any throwing star, criss, or vorpal is in left hand (timings are basically the same as shields so I just added them to the whitelist)
    3. added the option to tweak the double tap window for wingsmash and gravity jump in the config (just a nice QoL/accessibility option)
    4.



Known issues:

    1. input reading fails on octoshock cores (probably wont be fixed, just use the nymafast core like a normal person)
    2. UI elements still visible after save-quit, during load screens, and during certain cutscenes
    3. UI elements sometimes not reappear until a room transition after leaving the shop menu


Planned changes/improvements: (no promises here)

    1. figure out how to recode entire input system to not be reliant on referenc IDs in config and read direct from bizhawk {high priority, ongoing}
    2. slim down amount of local variables throughout the code to allow new features {high priority, ongoing}
    3. add a setup menu so that users dont need to edit config directly unless debugging {low priority}
    4. automatic updates without destroying player configs {low priority, wtf even is a netcoding lol}
    5. add analog stick and L3/R3 support (right for stick spell wheel???) {low priority}
    6. add next/prev spell buttons for better controller Qol {medium priority}
    7. add UI indicator for powerful items like duplicator, alucard + rod/mablung, criss/vorpal {low priority}
    8. Make proper thematic UI for LC/manna/gold counts {low priority}
    9. cheat option to give familiars lvl50 on pick-up (let them actually use special abilities for fun sake?) {low-priority}

